export default () => {
  const openWindow = () => {

  };

  return (
    <div>
      <h1>Electron React Rspack</h1>
      <button onClick={openWindow}>
        open sub window
      </button>
    </div>
  );
};
